README.md
.latexmkrc # currently I don't use LaTeX
.tmux.conf
.config/tridactyl

{{ if eq (. | dig "cp" "library" "") "" }}
.config/cp-template
{{ end }}

{{ if eq .chezmoi.os "windows" }} # use the file only windows
.zshrc
{{ end }}
{{ if ne .chezmoi.os "windows" }} # use the file only windows
AppData
Documents
{{ end }}

{{ if ne .chezmoi.os "darwin" }} # ignore if this is not mac (i.e. ignore if this is windows or linux)
Library
{{ end }}

{{ if ne .chezmoi.os "linux" }} # ignore if this is not linux (i.e. ignore if this is windows or mac)
.config/lazygit
.chezmoiscripts/linux/**

.xprofile
{{ end }}


{{ if ne .chezmoi.osRelease.name "Arch Linux" }}
.config/awesome
.config/alacritty
.config/rofi
.config/zathura
.xinitrc
.xprofile
{{ end }}

{{ if eq .chezmoi.osRelease.name "Kali GNU/Linux" }}
.zshrc
.config/starship.toml
{{ end }}
